dependencies {
	compile project(":portal:portal-search-elasticsearch")

	provided group: "org.elasticsearch", name: "elasticsearch", version: "2.0.0"
}

liferay {
	deployDir = file("${liferayHome}/osgi/modules")
}