File portalImplClassesDir = file("../../../../../../portal-impl/classes")
File portalServiceClassesDir = file("../../../../../../portal-service/classes")

if (portalImplClassesDir.exists() && portalServiceClassesDir.exists()) {
	dependencies {
		provided files(portalImplClassesDir, portalServiceClassesDir)
	}
}
else {
	dependencies {
		provided group: "com.liferay.portal", name: "portal-impl", version: liferay.portalVersion
		provided group: "com.liferay.portal", name: "portal-service", version: liferay.portalVersion
	}
}

liferay {
	deployDir = new File(appServerPortalDir, "WEB-INF/lib")
}